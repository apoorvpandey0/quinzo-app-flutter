import 'package:flutter/material.dart';

class SubjectTestsScreen extends StatelessWidget {
  static const routeName = '/subject-tests';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
